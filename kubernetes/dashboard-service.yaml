apiVersion: v1
kind: Service
metadata:
  name: future-ai-systems-dashboard
spec:
  type: ClusterIP
  selector:
    app: future-ai-systems-dashboard
  ports:
    - port: 8501
      targetPort: 8501
      protocol: TCP
