apiVersion: v1
kind: Service
metadata:
  name: future-ai-systems-api
spec:
  type: ClusterIP
  selector:
    app: future-ai-systems-api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
